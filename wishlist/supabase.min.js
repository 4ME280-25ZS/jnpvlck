/* Supabase UMD loader for browser */
window.supabase = (function(){
  /* Minimal mock for demo, replace with real UMD code for production */
  return {
    createClient: function(){
      throw new Error('Supabase UMD knihovna zde není vložena. Pro ostrý provoz stáhněte skutečný soubor z https://cdn.jsdelivr.net/npm/@supabase/supabase-js/dist/umd/supabase.min.js');
    }
  };
})();
